#include "validator.h"

void run() {
	int n = Int(1, 100000);
    Space();
    int r = Int(1, 1000000000);
	Endl();
    for(int c1 = 0; c1 < n; c1++){
        int sun = Int(0, 1000000000);
        Space();
        int water = Int(0, 1000000000);
        Endl();
    }
}